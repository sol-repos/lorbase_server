const { describe, it } = require("node:test");
const assert = require("node:assert");
const VarInt = require('../VarInt');

describe('VarInt class', () => {
    // TODO: Add tests for VarInt class
});
